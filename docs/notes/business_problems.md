# Business problems — relations

- Relation can be one-way (A loves B, B doesn't) or mutual (friends)
- Character can relate to itself (self-relation valid)
- Two characters can have multiple relations simultaneously (siblings + enemies)
- Same undirected relation must not duplicate (A-B "friends" == B-A "friends")
